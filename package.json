{
  "name": "cypress-project-1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:all-browsers": "npx cypress run --headed --browser chrome  && npx cypress run --headed --browser firefox && npx cypress run --headed --browser edge"
  },
  "author": "Sayid",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.16.0"
  }
}
